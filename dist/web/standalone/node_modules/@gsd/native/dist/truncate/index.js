"use strict";
/**
 * Line-boundary-aware output truncation (native Rust).
 *
 * Truncates tool output at line boundaries, counting by UTF-8 bytes.
 * Three modes: head (keep end), tail (keep start), both (keep start+end).
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.truncateTail = truncateTail;
exports.truncateHead = truncateHead;
exports.truncateOutput = truncateOutput;
const native_js_1 = require("../native.js");
/**
 * Keep the first `maxBytes` worth of complete lines.
 */
function truncateTail(text, maxBytes) {
    return native_js_1.native.truncateTail(text, maxBytes);
}
/**
 * Keep the last `maxBytes` worth of complete lines.
 */
function truncateHead(text, maxBytes) {
    return native_js_1.native.truncateHead(text, maxBytes);
}
/**
 * Main entry point: truncate tool output with head/tail/both modes.
 */
function truncateOutput(text, maxBytes, mode) {
    return native_js_1.native.truncateOutput(text, maxBytes, mode);
}
